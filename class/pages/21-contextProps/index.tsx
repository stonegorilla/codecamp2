import ContextProps from "../../src/components/units/21-contextProps/ContextProps.container";

export default function ContextPropsPage() {
  const isEdit = true;

  return <ContextProps isEdit={isEdit} />;
}
