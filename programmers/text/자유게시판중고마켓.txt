자유게시판, 중고마켓, 마이페이지 이런 것들은 솔직히
이제 거의 다 배웠으니, 다 만들 수 있다. 

getDebounce = _.debounce((data) ~~
어쩌고 저쩌고 

npm.js 에 들어가서 uuid 로 들어가라 
unique id 를 만들어라 
npm install uuid 를 하면 
node_modules 에 들어갈 것이다. 

props, prev  이런 것들은 이름을 바꾸어 주어도 좋다. 
컴포넌트 프리젠터 

여러 언어에서 정규표현식을 사용할 수 있게끔 해놓았다. 
그 정규표현식을 사용하려면 언어를 만드는 회사? 쪽에서 
만들어놓아야 한다. 
정규표현식 만든 쪽에서는 그냥 정규표현식만 만든다. 
(뭐 확실하게는 모르겠고 그냥 그렇다고 한다. ) 

다시한번, 한 컴포넌트 안에 수정하기, 등록하기모두 만드는 것을
음미한다. isEdit 을 props로 이용해서 presenter 까지 보냈다. 

하지만, 이제는 isEdit를 바로 presenter로 보낼 수 있다.  


redux